# Everything here is marketing copy written to be repeated, so every crawler
# and every answer engine is welcome.
#
# The named groups are a guard, not a workaround. Cloudflare's managed robots.txt
# used to be prepended to this file at the edge and disallowed GPTBot, ClaudeBot,
# Google-Extended and five others; it was turned off in the dashboard (#43), so
# what follows is the whole file again. Naming them here means a managed block
# switched back on is contradicted rather than silently obeyed — repeating a
# user-agent merges the groups, and an Allow of equal specificity wins the
# tie-break.

User-agent: *
Allow: /

# Search and AI answer surfaces, named because a managed block would name them.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: meta-externalagent
User-agent: CCBot
User-agent: Bytespider
User-agent: DuckAssistBot
User-agent: MistralAI-User
User-agent: cohere-ai
Allow: /

Sitemap: https://execil.net/sitemap.xml
