.DS_Store
node_modules/
dist/
dist-ssr/
.output/
.wrangler/
.tanstack/
.nitro/
*.local

# env — examples are committed, real values are not
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# OG image render font — a TTF from the Satoshi kit, used only by
# scripts/og-image.py because Pillow cannot read WOFF2. Not shipped.
scripts/.og-font/
