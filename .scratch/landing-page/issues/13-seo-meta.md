# 13 — What SEO and sharing metadata ships, and how is it generated?

Type: grilling
Status: open
Blocked by: 03

## Question

Minimal SEO is in scope. Decide:

- Title, meta description, canonical, robots and Open Graph and Twitter tags, with the strings in `messages.json`.
- The single static OG image: dimensions, how it is produced (hand-made asset versus generated at build), where it lives.
- `sitemap.xml` and `robots.txt` for a one-page site: hand-written static files or generated at build.
- Landmark structure to confirm semantic tags: header, nav, main, sections with headings, footer, address.
- What is deferred: structured data (already in fog).

Produce the head-tag list as the resolution.
